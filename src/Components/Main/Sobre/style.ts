import styled from 'styled-components'

export const About = styled.div`
    display: flex;
    justify-content: space-around;
    align-items: center;
    overflow: hidden;

    > img {
        width: 30%;
        min-width: 50px;
        border-radius: 50%;
     }

`